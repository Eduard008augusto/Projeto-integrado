import 'package:flutter/material.dart';

void main(){
  runApp(const AtualizarPerfil());
}

class AtualizarPerfil extends StatelessWidget {
  const AtualizarPerfil({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}